export class Items {
    id!: number;
    name!: string[];   // store multiple items here
    price!: number[];  // store price per item
    count!: number[]; // store count per item
    username!: string 
}[] = [];